checkpoint:
  path: outputs/checkpoints/mlp_mouth.pt
onnx:
  path: outputs/models/mlp_mouth.onnx
  model_type: mlp
benchmark:
  feature_dim: 70
  frames: 256
  warmup: 10
  repeat: 100
  seed: 42
output:
  report_path: outputs/reports/benchmark_report.json
logging:
  log_path: outputs/logs/benchmark.log

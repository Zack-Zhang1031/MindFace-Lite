checkpoints:
  original: outputs/checkpoints/grid_mlp_mouth.pt
  pruned: outputs/checkpoints/grid_mlp_mouth.pruned.pt
benchmark:
  feature_dim: 70
  frames: 256
  warmup: 10
  repeat: 100
  seed: 42
output:
  report_path: outputs/reports/pruned_benchmark.json
logging:
  log_path: outputs/logs/benchmark_pruned.log
